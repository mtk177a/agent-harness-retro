# AGENTS.md（日本語参考訳）

これは [`AGENTS.md`](AGENTS.md) の日本語参考訳です。\
英語版が正式な指示であり、この訳との差異がある場合は英語版を優先してください。\
このファイルは独立した指示の情報源ではありません。

このファイルは、`agent-harness-retro` で作業するエージェント向けに、リポジトリ固有の指示を定めています。

## 目的

`agent-harness-retro` は、コーディングエージェントのセッション証拠を使い、利用者が管理する外側のハーネスを事後的に評価します。

このリポジトリは、振り返りの概念、所見、証拠の関係、提案、分析と任意のハーネス変更の境界を扱います。

プロバイダー固有の生セッションへのアクセスは扱いません。\
その責務には `agent-sessions` を使用してください。

## 先に読むもの

重要な設計または実装の変更を行う前に、次を読んでください。

1. `README.md`
2. `docs/architecture.md`
3. `docs/glossary.md`
4. 変更に直接関係する ADR

網羅性を増やすためだけに、無関係なファイルを調べないでください。

## 公開情報の境界

このリポジトリは公開されています。

次のものを追加しないでください。

- 非公開リポジトリの名前またはリンク
- 非公開 Issue または Project への参照
- 個人または勤務先固有の文脈
- 実際のコーディングエージェントセッション内容
- 非公開環境にある実際の外側ハーネスの内容
- 資格情報または認証に使う情報
- 実在するユーザー名、ホスト名、ホームディレクトリ、マシン固有のパス

非公開の利用から得た要件を取り込めるのは、その非公開の情報源に依存しない一般的な製品要件として表現した後に限ります。

テストと例には、合成したリポジトリ、セッション、ハーネス成果物、識別情報を使用しなければなりません。

## 中核アーキテクチャの制約

明示的なアーキテクチャ上の決定で置き換えられない限り、次を維持してください。

- プロバイダー固有のセッションアクセスは `agent-sessions` の責務です。
- プロバイダーの生トランスクリプトを、このリポジトリまたは振り返り状態にコピーしません。
- 振り返りの対象は、ベンダーが管理する組み込みハーネスの内部ではなく、利用者が管理する外側のハーネスです。
- グローバルとリポジトリローカルのハーネス範囲には、同じ中核の振り返りモデルを使います。
- セッション証拠、振り返りの所見、実体化されたハーネス成果物は、別々の層として扱います。
- 所見があっても、変更が必要とは限りません。
- 頻度だけでは、永続的なハーネス指針を正当化できません。
- 分析と実体化は分離したままにします。
- 外部のハーネス成果物を、明示的な承認なしに変更しません。
- イベントごとに継続的な所見を生成する方式は、既定のアーキテクチャではありません。
- 非公開の実行時状態を公開リポジトリに保存しません。

後で役立つ可能性があるという理由だけで、バックグラウンド監視、トランスクリプトの自動取り込み、ハーネスの自動書き換え、または汎用的なセッションデータベースを追加しないでください。

## 証拠

重要な所見は、証拠まで追跡できなければなりません。

次のような安定した参照を優先してください。

- `agent-sessions` の情報源を示す識別子
- 検証済みの情報源の版
- 範囲を限定したイベントへの参照
- 影響を受ける範囲
- 所見に関係する現在のハーネス成果物

所見を監査可能にする目的だけで、完全な生トランスクリプトを保存しないでください。

生成された要約を、その要約の基になる直接観測した情報源より強い証拠として扱わないでください。

次を区別してください。

- 直接観測した動作
- 解釈
- 仮説
- 提案
- 未検証の前提

## ハーネスの範囲

ハーネスの変更を提案する前に、適切な範囲を決定してください。

候補となる結果には、次があります。

- グローバルの外側ハーネス
- リポジトリハーネス
- 既存の一つの Skill
- 一つの Hook または同じ条件で同じ結果を返す検査
- 基盤となるプロジェクトのコードまたは文書
- プロバイダーが管理する組み込みの動作
- 永続的な変更を行わない

複数の文脈にまたがる証拠がない限り、リポジトリローカルの動作をグローバルな規則へ昇格させないでください。

元のセッションを超えて成り立つ理由がない限り、一回限りの利用者修正を永続的なハーネス規則へ昇格させないでください。

## 制御面の選択

既定で指示を追加しないでください。

修正を提案するときは、関係する制御面を比較してください。

例を挙げます。

- 永続的な行動指針 → 指示または規則
- 条件付きの複数手順から成る作業の流れ → Skill
- 機械的に強制できる介入 → Hook または同じ条件で同じ結果を返す検査
- リポジトリの正しさに関する不変条件 → テストまたは CI
- 利用者が制御する実行時の動作 → 設定または権限
- 基準となるプロジェクト知識 → プロジェクト文書

並列する仕組みを作るより、適切な既存の仕組みを変更するほうを優先してください。

## 所見と提案

所見と提案する解決策を分けてください。

所見は、ハーネス成果物を処方する前に、観測したパターンと証拠を記すべきです。

提案には、次を示すべきです。

- 対象範囲
- 対象の成果物または制御面の種類
- 期待する改善
- 関係するトレードオフ
- 提案を裏付ける証拠
- 変更後に必要な検証または評価

却下と変更なしも有効な結果です。

## 実体化

- 振り返りで改善候補が見つかっただけで、外部のハーネス成果物を変更しないでください。
- 実体化を支援する機能が存在する場合でも、書き込み操作の前に明示的な承認を求めてください。
- 対象リポジトリの権限、指示、レビュー要件、検証コマンドを維持してください。
- 対象リポジトリで何を正式な更新元とするかという決まりを迂回しないでください。

## 実行状態

振り返りの状態は非公開の実行時データです。

次をコミットしないでください。

- 確認済みセッションの記録
- 非公開セッションに由来する所見
- 提案に関する判断
- 利用者固有のハーネス一覧
- 非公開の証拠

永続的な表現と保存形式は、製品要件で許される限り小さく保たなければなりません。

必要性が観測されない限り、高頻度の書き込み、バックグラウンド同期、またはデータベースを導入しないでください。

## 依存関係の境界

コーディングエージェントのセッションデータは、`agent-sessions` が公開する機械可読の仕様を通じて取得してください。

必要な機能が `agent-sessions` に属するかを先に判断せず、上流の不足を回避するために Codex、Claude Code、その他のプロバイダーのトランスクリプトを解析する機能を、このリポジトリへ直接追加しないでください。

## 言語

英語は、公開リポジトリの文書と規則における正式な言語です。\
維持対象の日本語ファイルは参考訳にすぎず、独立した要件または権限を定めません。\
日本語参考訳と英語の正式な情報源が異なる場合は、英語の情報源を優先します。

維持対象のファイル組と同期規則は、`docs/localization.md` に定められています。\
維持対象の英語の正式ファイルを追加または変更するときは、同じ変更内で、その日本語参考訳を作成またはレビューするために、リポジトリローカルの `.agents/skills/maintain-japanese-references` Skill を使用してください。\
英語版の編集が日本語の意味に影響しない場合は、参考訳を変更せず、その理由を Pull Request の Validation または Risks / Follow-up セクションに記録してください。\
この維持対象の一部として ADR を翻訳しないでください。

英語を既定とする対象は、次のとおりです。

- コードと識別子
- コードコメント
- CLI または Skill の公開インターフェース
- コミットメッセージの要約
- リリースノート
- 正式版の README、文書、用語集、ADR、Skill、リポジトリ方針ファイル

保守者が作成する公開 Issue と Pull Request では、英語のタイトルと短い英語の `Summary` を使用します。\
その他の本文セクションとコメントは、既定で日本語とし、必要に応じて英語で記述できます。\
外部の貢献者に日本語を要求せず、本文全体を英語と日本語の両方で書くことも要求しません。

`AGENTS-ja.md` はこのファイルの参考訳であり、独立した指示の情報源ではありません。

## 変更の規律

必要な振り返り機能を確立する、最も小さい仕組みを優先してください。

具体的な要件で必要になる前に、分類体系、評価モデル、永続化の仕組み、処理を調整する枠組み、または自動実体化の層を作らないでください。

アーキテクチャ上の不変条件を変更するときは、次を行ってください。

1. 影響を受ける ADR を特定する。
2. 受け入れられた決定が引き続き成り立つかを判断する。
3. 必要に応じて ADR を更新または置き換える。
4. README、用語集、アーキテクチャ文書、実装、テストの整合を保つ。

有効な決定を説明するために必要な場合を除き、現在の状態を表す製品文書に実装履歴を追加しないでください。

## 検証

合成した証拠とハーネスのテスト用データを使用してください。

手作業で非公開部分を除去しても、実際の非公開セッションをコミットする評価データに使用しないでください。

変更が振り返りの品質に影響する場合は、無関係な評価スイートを広げるのではなく、失敗し得る判断境界をテストしてください。

完了を報告する前に、最終差分に非公開情報やマシン固有のデータが誤って含まれていないかを必ず確認してください。
