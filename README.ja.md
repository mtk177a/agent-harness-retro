# agent-harness-retro

これは [`README.md`](README.md) の日本語参考訳です。\
英語版が正式な README であり、差異がある場合は英語版を優先してください。

`agent-harness-retro` はコーディングエージェントのセッション証拠を使い、ユーザーが管理するハーネスを振り返って評価し、証拠に基づく改善を提案する。\
対象は、コーディングエージェントの周囲にあり、ユーザーまたはリポジトリが管理する指示、Skill、Hook、設定、検証、その他の制御手段である。

> 状態: 提案のみを行う Skill を実装済み。
> 実際の `agent-sessions` との連携、非公開の継続状態、パッケージ化は今後の課題である。

## 目的

コーディングエージェントのセッションには、外側のハーネスが実際にどのように機能したかを示す証拠が含まれる。\
ユーザーによる繰り返しの修正、不要な再試行、対象範囲の逸脱、検証漏れ、効果のない指示、Skill の振り分けの失敗、繰り返し成功する手順は、コーディングエージェントを取り囲むハーネスを改善する機会を示すことがある。\
`agent-harness-retro` は、その証拠を構造化して振り返るための層である。

```text
coding-agent sessions
        │
        ▼
   agent-sessions
        │
        ▼
agent-harness-retro
        │
        ├── findings
        ├── evidence
        └── proposals
                │
                ▼
      user-controlled outer harness
```

`agent-harness-retro` は、Codex、Claude Code、その他のコーディングエージェント製品が提供する、ベンダー管理の組み込みハーネスを変更しない。

## 外側のハーネスとは

このリポジトリでは、**外側のハーネス** をコーディングエージェント製品の周囲に設けられた、ユーザーが管理する層と定義する。

対象範囲に応じて、次を含む場合がある。

- `AGENTS.md`、`CLAUDE.md`、ルールファイル。
- Skill。
- Hook。
- ユーザーが管理する設定と権限。
- カスタムエージェントやサブエージェントの定義。
- MCP とプラグインの設定。
- エージェントが使うリポジトリ文書。
- 同じ条件で同じ結果を返す検証、CI、評価用の成果物。

このリポジトリで使う正確な用語は [用語集](docs/ja/glossary.md) で定義する。

## 対象範囲

同じ振り返りモデルを、2 つの主要な対象範囲に適用する。

### グローバルな外側のハーネス

複数のリポジトリまたはセッションに適用するための、ユーザー管理のハーネス成果物。

### リポジトリのハーネス

1 つのリポジトリまたはプロジェクトが所有するハーネス成果物。

リポジトリのハーネスにも、特定のリポジトリに依存しない振り返りモデルを適用する。\
どのリポジトリも振り返りの対象になり得る。

## 証拠の出所

`agent-harness-retro` は [`agent-sessions`](https://github.com/mtk177a/agent-sessions) が提供する、安定した `v1` の正規化済みセッション証拠の公開仕様を利用する。\
プロバイダーが所有する Codex、Claude Code、その他の生のセッション形式を直接解析しない。\
これにより、プロバイダーへのアクセスとハーネスの解釈を別の責務に保つ。

## 提案専用 Skill

初期実装は [`outer-harness-retrospective`](skills/outer-harness-retrospective/SKILL-ja.md) Skill である。\
この Skill は、意図的に範囲を限定した正規化済みのセッション証拠、対象範囲、関連する現在の外側のハーネスの観測結果を受け取る。\
観測結果、所見、原因仮説、対象範囲の評価、提案を分離し、`no_change`、`reject`、`insufficient_evidence` を通常の判断として返せる。

Skill はプロバイダーのセッションを取得せず、振り返りの状態を保存せず、外側のハーネスの成果物を変更しない。\
合成評価のケースは [`evals/outer-harness-retrospective/cases.md`](evals/outer-harness-retrospective/cases.md) で管理する。

## 振り返りモデル

初期モデルは次を分離する。

```text
session evidence
      ↓
retrospective analysis
      ↓
finding
      ↓
scope and likely cause
      ↓
improvement proposal
```

所見があるだけで、ハーネスの変更が必要になるわけではない。

有効な結果には次がある。

- 既存のハーネス成果物を改善する。
- ルールをより適切な制御手段へ移す。
- 既存の仕組みを簡素化または削除する。
- 新しい仕組みを作る。
- 現在の設計を維持する。
- 根拠が不十分な所見を却下する。

頻度だけでは、恒久的なハーネスの変更を正当化する十分な証拠にならない。

## 証拠に基づく提案

提案には、その根拠を説明できるだけの情報を残す。

所見に応じて、次を含める場合がある。

- 参照元。
- 検証済みの参照元の版。
- 代表的なイベント参照。
- 影響を受けるリポジトリまたは対象範囲。
- 再発情報。
- 代替説明。
- 確度と未解決の不確実性。

過去のセッション内容は、信頼されていない入力として扱う。

## 状態

`agent-sessions` とは異なり、`agent-harness-retro` は非公開の継続状態を必要とする場合がある。

例として次がある。

- 確認済みの参照元の版。
- 所見。
- 証拠参照。
- 承認または却下された提案。
- 検証状態。

この状態は非公開の実行時データである。\
公開リポジトリには保存せず、プロバイダーの生のトランスクリプト全体のコピーも含めない。\
保存形式は初期アーキテクチャで定めるものではなく、実装要件が分かってから決める。

## 実体化

振り返り分析とハーネスの変更は別の責務である。

初期設計は提案を行うことに限定する。

所見に基づいて、次を通知なく変更してはならない。

- 指示。
- Skill。
- Hook。
- 設定。
- CI。
- 評価用の成果物。
- その他のハーネス成果物。

実体化を実装する場合、外部のハーネス成果物を変更する前に、ユーザーが提案された変更を明示的に承認しなければならない。

## 設計原則

### ルールより証拠を優先する

証拠が一般化を正当化しない場合、孤立した事例から恒久的なハーネスのルールを作らない。

### 変更前に対象範囲を確定する

問題が次のどこに属するかを判断する。

- 組み込みハーネス。
- グローバルな外側のハーネス。
- リポジトリのハーネス。
- 1 つの Skill または Hook。
- 基盤となるプロジェクト実装。
- 永続的なハーネスの仕組みを作らない。

このリポジトリが変更候補にできるのは、ユーザーが管理する外側のハーネスの対象だけである。

### 適切な制御面を優先する

自然言語の指示が、常に正しい修正とは限らない。

所見は、代わりに次のいずれかに属する場合がある。

- Skill。
- Hook。
- 同じ条件で同じ結果を返すスクリプト。
- CI またはテスト。
- ユーザーが管理する設定。
- プロジェクト文書。
- その他、基準となる既存の情報源。

### 実体化の前に提案する

分析は、自動的な書き込みを必要としなくても有用でなければならない。

### 候補を継続的に生成しない

既定のアーキテクチャは、すべてのツール呼び出しまたはセッションイベントから所見を継続的に生成しない。\
振り返り分析は、明示的に依頼された場合、または別途意図的に起動された場合に行う。

### 保持するセッションデータを最小化する

完全なトランスクリプトの永続的なコピーではなく、参照と派生した証拠を保存する。

## 公開・非公開の境界

これは公開リポジトリである。

公開成果物は一般化された製品の挙動だけを記述する。

次のものは含めない。

- 非公開リポジトリの名前やリンク。
- 非公開の Issue または Project の参照。
- 個人または雇用者固有の文脈。
- 実際のセッション内容。
- 非公開のハーネス内容。
- 実在するユーザー名、ホスト名、マシン固有のパス。

非公開の利用から得た要件は、非公開の出所に依存しない一般的な製品要件として表現した後に限り取り込む。

## 対象外

`agent-harness-retro` は次のものではない。

- コーディングエージェントのセッションパーサー。
- `agent-sessions` の代替。
- プロバイダーのテレメトリ収集機能。
- 汎用メモリシステム。
- バックグラウンド監視デーモン。
- 自動プロンプト調整機能。
- セッションごとにハーネス成果物を書き換えるシステム。
- プロバイダーが所有する組み込みハーネスの開発作業の代替。
- 一般的なプロジェクトアーキテクチャのレビュー担当。
- 繰り返される操作をすべて候補 Skill として扱うシステム。

## 文書

- [アーキテクチャ](docs/ja/architecture.md)
- [用語集](docs/ja/glossary.md)
- [ローカリゼーション方針](docs/ja/localization.md)
- [ADR 0001: Target the user-controlled outer harness](docs/decisions/0001-target-the-user-controlled-outer-harness.md)
- [ADR 0002: Separate session evidence, findings, and materialization](docs/decisions/0002-separate-session-evidence-findings-and-materialization.md)

## ライセンス

MIT
